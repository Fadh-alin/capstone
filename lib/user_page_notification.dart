import 'package:flutter/material.dart';

class UserNotifications extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    body: Center(child: Text('Coding from Faiq', style: TextStyle(fontSize: 60))),
  );
}